apiVersion : apps/v1
kind: Deployment
metadata:
  name: pipelinessampleapp 
spec:
  replicas: 1
  selector:
    matchlabels:
      app: pipelinessampleapp
  template:
    metadata:
      labels:
        app: pipelinessampleapp 
    spec:
      containers:
        - name: pipelinessampleapp 
          image: uniqueacrnamehere.azurecr.io/pipelinessampleapp
          ports:
          - containerPort: 8080
